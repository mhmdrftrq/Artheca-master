name: artheca
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # Versi ini lebih aman biar gak error

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.0
  http: ^1.1.0
  flutter_staggered_grid_view: ^0.7.0
  supabase_flutter: ^2.0.0
  google_fonts: ^6.2.1
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  # --- TAMBAHKAN INI AGAR AUTOMATION JALAN ---
  integration_test:
    sdk: flutter
  # ------------------------------------------
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/image/